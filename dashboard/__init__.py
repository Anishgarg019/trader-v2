"""Cloud dashboard: a Postgres-backed store the Windows agent POSTs to, and a Streamlit
app (Streamlit Community Cloud) that reads it. READ-ONLY performance data only — never Kite
credentials or order capability."""
